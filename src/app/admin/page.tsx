export default async function Admin() {
  return (
    <div>
      <p>
        Select an area from the admin menu to manage users, relationships,
        tasks, totals, or specialties.
      </p>
    </div>
  );
}
